Must have database set up in order to run!!!

Run command prompt in project dir as admin:

type in
' mysql -u root -p '

it will ask for password type in:

' 123adjr! '

run another command prompt in porject directory then type in:

' nodemon app.js '

This should connect to database.

Then enter this URL:

' localhost:8080/ '
